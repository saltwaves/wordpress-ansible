---
- name: start php-fpm
  service:
    name: "{{ php_service_name }}"
    state: started

- name: reload php-fpm
  service:
    name: "{{ php_service_name }}"
    state: reloaded

- name: restart php-fpm
  service:
    name: "{{ php_service_name }}"
    state: restarted
